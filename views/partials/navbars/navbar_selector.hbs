
{{#if (navbar_requested_is_clear navbar)}}
    {{> clear_navbar}}
{{else if (navbar_requested_is_dark navbar)}}
    {{> dark_navbar}}
{{else if (navbar_requested_is_default navbar)}}
    {{> default_navbar}}
{{/if}}